import { StyleSheet } from 'react-native';
import platform from '../../../native-base-theme/variables/platform';

const styles = StyleSheet.create({
  viewContainer: {
    marginVertical: platform.fiveScale,
  },
});

export default styles;
